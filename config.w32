// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("yal", "for yal support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("yal", "enable yal support", "no");

if (PHP_YAL != "no") {
	EXTENSION("yal", "yal.c");
}

